# 77X 线路走向 / 高度图

仅 **77X**（含详情里的 **77XA**）使用此目录，其他线路请勿放置图片。

| 文件名 | 含义 |
|--------|------|
| `path.png` | 走向（地图路线，图二） |
| `height.png` | 高度（海拔剖面，图一） |

也支持 `.webp` / `.jpg`。放好文件后执行 `npm run build:only`，构建会自动更新 manifest，详情页才会出现按钮。
